<IfModule mod_rewrite.c>
   RewriteEngine On
   # Exclude WhatsApp server path from Laravel redirect
   RewriteCond %{REQUEST_URI} !^/whatsapp-server-temp
   RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
